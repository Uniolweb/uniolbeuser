### separate label and description with ":"
### Things added in later versions, add at the bottom
### Use category "pages" or "files" or "general"

# cat=pages; type=string; label=Page types (doktype) which should be viewable in FE (csv string)
visiblePageTypes = 1

# cat=pages; type=string; label=Do not show these pages (csv string)
ignorePageDbMounts =

# cat=BE Groups; type=string; label=Use only BE groups which match this title (use % for wildcards as in LIKE)
beGroupsTitleLike =

# cat=data protection; type=boolean; label=Always blur names of editors: They will only be visibly blurred, name is still in HTML-code!
blurBeUsernames = 0
